{{- /*
ExternalSecret for wealist-shared-secret
Syncs AWS SSM Parameters to K8s Secret that services reference.
*/ -}}
{{- if .Values.externalSecrets.wealistSharedSecret.enabled }}
{{- $prefix := .Values.externalSecrets.wealistSharedSecret.parameterPathPrefix }}
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: {{ .Values.externalSecrets.wealistSharedSecret.name }}
  namespace: {{ .Values.global.namespace }}
spec:
  refreshInterval: {{ .Values.externalSecrets.wealistSharedSecret.refreshInterval }}
  secretStoreRef:
    name: {{ .Values.secretStore.name }}
    kind: ClusterSecretStore
  target:
    name: {{ .Values.externalSecrets.wealistSharedSecret.name }}
    creationPolicy: Owner
  data:
    # -------------------------------------------------------------------------
    # Google OAuth Credentials
    # -------------------------------------------------------------------------
    - secretKey: GOOGLE_CLIENT_ID
      remoteRef:
        key: {{ $prefix }}/google-oauth/client-id
    - secretKey: GOOGLE_CLIENT_SECRET
      remoteRef:
        key: {{ $prefix }}/google-oauth/client-secret

    # -------------------------------------------------------------------------
    # JWT Secret
    # -------------------------------------------------------------------------
    - secretKey: JWT_SECRET
      remoteRef:
        key: {{ $prefix }}/jwt/secret

    # -------------------------------------------------------------------------
    # Database Password (shared for all services)
    # -------------------------------------------------------------------------
    - secretKey: DB_PASSWORD
      remoteRef:
        key: {{ $prefix }}/database/user-password

    # -------------------------------------------------------------------------
    # Redis Password
    # -------------------------------------------------------------------------
    - secretKey: REDIS_PASSWORD
      remoteRef:
        key: {{ $prefix }}/redis/password

    # -------------------------------------------------------------------------
    # S3/MinIO Credentials
    # -------------------------------------------------------------------------
    - secretKey: S3_ACCESS_KEY
      remoteRef:
        key: {{ $prefix }}/minio/access-key
    - secretKey: S3_SECRET_KEY
      remoteRef:
        key: {{ $prefix }}/minio/secret-key

    # -------------------------------------------------------------------------
    # Internal API Key
    # -------------------------------------------------------------------------
    - secretKey: INTERNAL_API_KEY
      remoteRef:
        key: {{ $prefix }}/internal/api-key
{{- end }}
